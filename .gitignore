node_modules
dist
dist-ssr
*.local
.vite

# Rust / Tauri
src-tauri/target
# Cargo.lock: app olduğu için commit edilir (deterministic builds)

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Build artefacts
*.log

# Tauri updater signing keys (NEVER commit!)
*.key
*.key.pub

# Release artefacts (sadece GitHub Release asset olarak yayınla)
release/
